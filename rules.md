# Project Rules

- Put new database schema/data migration statements for existing deployments in `backend/src/main/resources/db/alter_existing_global_settings_cleanup.sql`; do not add new migration-only changes directly to `backend/src/main/resources/db/schema.sql`.
